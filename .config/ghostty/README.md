# Ghostty Configuration

[Ghostty](https://ghostty.org/) is the primary terminal emulator in this setup. It is chosen for its performance, modern features, and easy configuration.

## Usage

Ghostty is mapped to `SUPER + T` by default.
